import { WrapResponsesInterceptor } from './wrap-responses.interceptor';

describe('WrapResponsesInterceptor', () => {
  it('should be defined', () => {
    expect(new WrapResponsesInterceptor()).toBeDefined();
  });
});
